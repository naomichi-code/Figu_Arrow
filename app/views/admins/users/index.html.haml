.container
  %h3.text-light.bg-secondary.mt-2.d-inline-block ユーザー確認画面
  %h4.my-5
    = "本日の登録者数 #{@user_count.count}"
  %table.table
    %thead
      %tr
        %th アカウント名
        %th
        %th
        %th アカウント作成日
        %th{:colspan => "2"}
    %tbody
      - @users.each do |user|
        %tr
          %td= user.account_name
          %td= "フォロー数：#{user.follower.count}"
          %td= "フォロワー数：#{user.followed.count}"
          %td= user.created_at.strftime('%Y/%m/%d %H:%M')
          %td
            =link_to 'アカウント詳細', user_path(user),class: 'btn btn-sm btn-success'
          %td
            = link_to '強制退会', admins_user_path(user),method: :delete, data: {confirm:'本当に退会させますか？'}, class: 'btn btn-sm btn-danger'