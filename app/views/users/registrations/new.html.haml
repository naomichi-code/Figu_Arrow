.col-sm-5.mx-auto.mt-5.shadow-lg.p-3.mb-5.bg-white.rounded{style: 'font-size:1em;'}
  %h4.text-center 会員登録画面
  = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    = render "users/shared/error_messages", resource: resource

    /以下アカウント登録内容
    .form-row.mt-4
      .form-group.col-sm
        = f.label :account_name,'アカウント名'
        = f.text_field :account_name, autofocus: true, class: 'form-control'
    /氏名
    %p.mb-1 氏名
    .form-row
      .form-group.col-sm
        = f.text_field :last_name, placeholder: '姓', class: 'form-control'
      .form-group.col-sm
        = f.text_field :first_name, placeholder: '名', class: 'form-control'
    %p.mb-1 氏名(カナ)
    .form-row
      .form-group.col-sm
        = f.text_field :last_name_kana, placeholder: 'セイ', class: 'form-control'
      .form-group.col-sm
        = f.text_field :first_name_kana, placeholder: 'メイ', class: 'form-control'
    /メールアドレス
    .form-row
      .form-group.col-sm
        = f.label :email
        = f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control', placeholder: 'abc1234@example.com'
    /パスワード
    .form-row
      .form-group.col-sm
        = f.label :password, 'パスワード'
        - if @minimum_password_length
          = f.password_field :password, autocomplete: "new-password", class: 'form-control',placeholder: "#{@minimum_password_length}文字以上の半角英数字を入力してください"
    .form-row
      .form-group.col-sm
        = f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control', placeholder: '確認用'
    /郵便番号
    .form-row
      .form-group.col-sm
        = f.label :postal_code,'郵便番号'
        = f.text_field :postal_code,class: 'form-control', placeholder: '123-1234'
    /住所
    .form-row
      .form-group.col-sm
        = f.label :address,'住所'
        = f.text_field :address, class: 'form-control'
    /電話番号
    .form-row
      .form-group.col-sm
        = f.label :phone_number,'電話番号'
        = f.text_field :phone_number, class:'form-control'
    .row
      .actions.col-sm-12.text-center.mt-3
        = f.submit "新規登録", class: 'mt-1 btn btn-secondary btn-lg btn-block'
    .text-center
      = render "users/shared/links"