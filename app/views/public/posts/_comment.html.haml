- post_comments.each do |post_comment|
  %table.table.table-borderless.bg-white
    %tr
      %td.text-center
        = link_to user_path(post_comment.user)  do
          = attachment_image_tag(post_comment.user, :image_icon, :fill, 50, 50, fallback: "no_image.jpg", size: "50x50", class: ' border')
          %br
        %span{style:'font-size:2px;'}= post_comment.user.account_name
      %td
        .pt-3=post_comment.comment
      - if post_comment.user == current_user
        %td
          .pt-3= link_to 'コメント削除', post_post_comments_path(post_comment),{class: 'btn-sm btn-danger', method: :delete, remote: true}
    %tr