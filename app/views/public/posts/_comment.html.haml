- post_comments.each do |post_comment|
  %table.table.table-borderless.bg-white
    %tr
      %td{style: "width: 20%;"}
        = link_to user_path(post_comment.user)  do
          = attachment_image_tag(post_comment.user, :image_icon, :fill, 50, 50, fallback: "no-image-icon.jpg", size: "50x50", class: ' border')
          %br
        %span.ml-3{style:'font-size:2px;'}= post_comment.user.account_name
      %td
        =post_comment.comment
      %td{style: "width: 30%;"}
        - if post_comment.user == current_user
          = link_to 'コメント削除', post_post_comments_path(post_comment),{class: 'btn-sm btn-danger', method: :delete, remote: true}
    %tr