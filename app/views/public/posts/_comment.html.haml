- post_comments.each do |post_comment|
  %tr
    %td
      = link_to user_path(post_comment.user)  do
        = attachment_image_tag(post_comment.user, :image_icon, :fill, 50, 50, fallback: "no_image.jpg", size: "50x50")
        = post_comment.user.account_name
    %td  = book_comment.comment
    - if post_comment.user == current_user
      %td = link_to 'コメント削除', post_post_comments_path(post_comment),{class: 'btn-sm btn-danger', method: :delete, remote: true}
  %tr